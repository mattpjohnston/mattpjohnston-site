name: Deploy Frontend

on:
  push:
    branches:
      - main
    paths:
      - "apps/web/**"
      - ".github/workflows/deploy-frontend.yml"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: deploy-frontend
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "apps/web/package-lock.json"

      - name: Install dependencies
        working-directory: apps/web
        run: npm ci

      - name: Build site
        working-directory: apps/web
        env:
          PUBLIC_COUNTER_API_URL: ${{ vars.PUBLIC_COUNTER_API_URL }}
        run: npm run build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_DEPLOY_ROLE_ARN }}
          aws-region: eu-west-1

      # only the _astro files have a hash in the name, everything else keeps
      # the same URL between builds
      - name: Sync hashed assets to S3 (cache forever)
        run: |
          aws s3 sync apps/web/dist/ s3://${{ vars.S3_BUCKET }} \
            --delete \
            --exclude "*" \
            --include "_astro/*" \
            --cache-control "public,max-age=31536000,immutable"

      - name: Sync everything else to S3 (revalidate)
        run: |
          aws s3 sync apps/web/dist/ s3://${{ vars.S3_BUCKET }} \
            --delete \
            --exclude "_astro/*" \
            --cache-control "public,max-age=0,must-revalidate"

      # probably droppable, the pages revalidate on their own now
      - name: Invalidate CloudFront
        run: aws cloudfront create-invalidation --distribution-id ${{ vars.CLOUDFRONT_DISTRIBUTION_ID }} --paths "/*"
